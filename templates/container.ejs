import { connect } from 'react-redux'
import <%= props.name %>Component from '../Components/<%= props.name %>/<%= props.name %>Component
// Styles
import styles from './Styles/<%= props.name %>Style'

const mapStateToProps = (state) => {
  return {
  }
}

const mapDispatchToProps = (dispatch) => {
  return {
  }
}

export default connect(mapStateToProps, mapDispatchToProps)(<%= props.name %>Component)
