import "react-native";
import * as React from "react";
import * as renderer from 'react-test-renderer';
import {FifthTabComponent} from '../../../App/Components/<%= props.name %>Tabs/FifthTab'
import {colorScheme} from "../../../App/Themes/Colors";

test("Renders Correctly", () => {
const tree = renderer.create(<FifthTabComponent locale='en' colorScheme={colorScheme(false)} />).toJSON();
expect(tree).toMatchSnapshot();
});
