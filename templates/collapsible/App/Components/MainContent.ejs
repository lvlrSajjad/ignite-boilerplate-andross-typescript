import * as React from 'react'
import {Component} from 'react';
import {connect} from 'react-redux'
import { Text, View} from 'react-native'

interface <%= props.name %>MainContentProps {

}

interface <%= props.name %>MainContentState {

}

export default class <%= props.name %>MainContent extends Component<<%= props.name %>MainContentProps,<%= props.name %>MainContentState> {
  componentWillMount() {
  }

  render() {
    return (
      <View>
        {new Array(40).fill().map((_, i) => (
          <View
            key={i}
            style={{
              backgroundColor: '#F5F5F5',
              padding: 10,
              borderBottomWidth: 1,
              borderBottomColor: '#E5E5E5'
            }}
          >
            <Text>{`Item ${i + 1}`}</Text>
          </View>
        ))}
      </View>
    );
  }
}
