import {<%= props.name %>Component,<%= props.name %>MainContent,<%= props.name %>ToolbarContent} from '../../App/Components/<%= props.name %>'

test("Renders Correctly", () => {
const tree = renderer.create(<<%= props.name %>Component/>).toJSON();
expect(tree).toMatchSnapshot();
});

test("Renders Correctly", () => {
const tree = renderer.create(<<%= props.name %>MainContent/>).toJSON();
expect(tree).toMatchSnapshot();
});

test("Renders Correctly", () => {
const tree = renderer.create(<<%= props.name %>ToolbarContent/>).toJSON();
expect(tree).toMatchSnapshot();
});
