import * as React from 'react';
import {Component} from 'react';
import { View, Text } from 'react-native'
import styles from './Styles/<%= props.name %>Style.rtl'
import {<%= props.name %>Props} from './index'

export default class <%= props.name %> extends Component<<%= props.name %>Props> {

  // // Defaults for props
  // static defaultProps = {
  //   someSetting: false
  // }

  render () {
    return (
      <View>
        <Text><%= props.name %> Component Rtl</Text>
      </View>
    )
  }
}
