import * as React from "react";
import Rtl from './<%= props.name %>.rtl';
import Ltr from './<%= props.name %>.ltr';
import {ColorScheme} from "../../Themes/Colors";


export interface <%= props.name %>Props {
isDarkMode?: boolean,
isRtl?:boolean,
colorScheme?:ColorScheme
}

export default (props:<%= props.name %>Props) => props.isRtl? <Rtl {...props} /> : <Ltr {...props} />;
